<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx
    x:Class="App.Flyout.Window"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App.Flyout"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winex="using:WinUIEx"
    mc:Ignorable="d">

    <TextBlock x:Name="FlyoutAnchor">
        <FlyoutBase.AttachedFlyout>
            <Flyout x:Name="FlyoutControl" Placement="Bottom" ShouldConstrainToRootBounds="False" >
                <Flyout.FlyoutPresenterStyle>
                    <Style TargetType="FlyoutPresenter"
                        BasedOn="{StaticResource DefaultFlyoutPresenterStyle}">
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="MinHeight" Value="0" />
                        <Setter Property="MinWidth" Value="0" />
                        <Setter Property="Padding" Value="0" />
                    </Style>
                </Flyout.FlyoutPresenterStyle>
                <StackPanel Width="32" Height="32" Padding="0,7,0,0">
                    <FontIcon x:Name="FlyoutContent" Glyph="&#xE985;" FontSize="16" />
                    <Border x:Name="FlyoutActiveIndicator" Height="2" CornerRadius="1" Margin="0,3,0,0"
                        Width="16" Background="{ThemeResource TextFillColorPrimary}" />
                </StackPanel>
                <Flyout.SystemBackdrop>
                    <DesktopAcrylicBackdrop />
                </Flyout.SystemBackdrop>
            </Flyout>
        </FlyoutBase.AttachedFlyout>
    </TextBlock>
    <Window.SystemBackdrop>
        <winex:TransparentTintBackdrop TintColor="Transparent" />
    </Window.SystemBackdrop>
</winex:WindowEx>
